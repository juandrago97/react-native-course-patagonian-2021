import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  bookThumbnail: {
    width: 90,
    height: 90,
    borderRadius: 30,
  },
  recommendedBookItem: {
    width: 90,
    marginHorizontal: 8,
  },
});
export default styles;
