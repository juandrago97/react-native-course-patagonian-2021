import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  detailList: {
    flex: 1,
  },
  row: {
    display: 'flex',
    flexDirection: 'row',
  },
});
export default styles;
